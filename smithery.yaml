# Smithery.ai MCP Server Configuration for TypeScript Runtime
# https://smithery.ai/docs/build/config

runtime: typescript
target: src/index.ts

# Environment variables (users will configure these during installation)
env:
  FIGMA_ACCESS_TOKEN: ""
  GITHUB_TOKEN: ""
  FIGMA_MCP_SERVER_URL: "http://127.0.0.1:3845/mcp"
